export const MONGODB_JSONS = {
  chat: 'ZG_JSONS', // MongoDB collection/namespace key for Altair chat records stored as JSON payloads
  swap: 'ZG_JSONS', // MongoDB collection/namespace key for Altair swap records stored as JSON payloads
};
